# mobile/ 클라이언트 환경변수 (EXPO_PUBLIC_* 만 번들에 노출됨 — 비밀키 절대 금지)
# 백엔드(B) REST + realtime/session 발급 엔드포인트 베이스 URL
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000

# B/C 미완성 구간을 contract mock 으로 대체할지 (1=mock, 0=실서버)
EXPO_PUBLIC_USE_MOCK=1
