{
  "name": "civiclens-server",
  "private": true,
  "type": "module",
  "comment": "Node 측(백엔드 server/ + scripts/) 워크스페이스. 모바일은 mobile/ 자체 package.json. ⚠️ 이 파일은 Day-0 공유 인프라 — main에 동기화 권장.",
  "scripts": {
    "type-check": "tsc --noEmit",
    "ingest": "tsx scripts/ingest-aihub.ts",
    "eval:build": "tsx scripts/build-eval-set.ts",
    "benchmark": "tsx scripts/run-benchmark.ts"
  },
  "dependencies": {
    "openai": "^4.77.0",
    "zod": "^3.24.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
